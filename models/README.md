# MediaPipe model

`gesture_recognizer.task` is the official MediaPipe Gesture Recognizer model. If it is removed, `vision_gesture_control.py` downloads the same model automatically on first run.

Upstream URL:

<https://storage.googleapis.com/mediapipe-models/gesture_recognizer/gesture_recognizer/float16/1/gesture_recognizer.task>
